#include <bits/stdc++.h>
using namespace std;

int main() {
    int h, w;
    cin >> h >> w;

    vector<string> c(h);
    for (int i = 0; i < h; i++) cin >> c.at(i);

    for (int i = 0; i < h; i++) {
        cout << c.at(i) << endl;
        cout << c.at(i) << endl;
    }

    return 0;
}
